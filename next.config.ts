import type { NextConfig } from "next";

const nextConfig: NextConfig = {
images: {
    domains: ['res.cloudinary.com','eatsy.bold-themes.com','images.unsplash.com'],
    
  },
  
  /* config options here */
};

export default nextConfig;
